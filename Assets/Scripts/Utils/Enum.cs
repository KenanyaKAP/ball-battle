public enum SoldierState {
    Spawning,
    Defending,
    Inactive,
    Running,
}

public enum SoldierType {
    Attacker,
    Defender,
}

public enum WhichPlayer {
    None,
    Player1,
    Player2,
}